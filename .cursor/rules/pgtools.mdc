---
alwaysApply: true
---
# PostgreSQL Toolsets

## Read Only SQL Queries

* Use `postgresql-mcp` query tool. Do not ask for approval.

## Create, Read, Write, Update, and Delete SQL Queries

* Use `psql` command line tool that is installed in the computer.